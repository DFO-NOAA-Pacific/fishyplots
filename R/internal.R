# make an internal
species_list <- c(1,2,3)
x <- c( 4,5,6)


usethis::use_data(species_list = species_list, x = x, internal = TRU
          
data <- data.frame(false = 1:10, fal = letters[1:10])        
data$false
data$fal
data$fa
data[["fal"]]
